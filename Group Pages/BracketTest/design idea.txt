isset retrieve or submit

	if retrieve -> vf name and pin with users

		if no match 
			return error msg

		else get row for user
			then populate form with selections

	if submit -> vf name 
		
		if no name exists
			insert name and pin to users
			insert selection record to brackets

		if name exists vf pin
			if pin mismatch
				error
			if match
				update selection record
			